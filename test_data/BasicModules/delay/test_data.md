# delay test data

This directory contains one parameter set tested against `simulation.toml`.

| Parameter | Value |
|-----------|-------|
| `LATENCY` | 1 |
| `BITWIDTH` | 3 |

The output (`sim_out.csv`) is the input (`sim_in.csv`) delayed by 1 clock cycle.
